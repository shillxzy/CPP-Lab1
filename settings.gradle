pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "${quarkusPluginId}" version "${quarkusPluginVersion}"
    }
}
rootProject.name = 'cross-lab1'
include 'user-service', 'artist-service', 'song-service', 'playlist-service', 'frontend-service'

